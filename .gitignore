# npx skills add defaults here for Cursor; we keep skills under .cursor/skills/ instead
.agents/

# Local Grafana credentials (Cloudflare account ID + analytics token)
grafana/.env

# Revenue diagnostic output (script/revpan-diagnostic) — revenue data, never commit
docs/revpan-diagnostic-*
